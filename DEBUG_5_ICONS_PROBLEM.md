# 🔍 ДИАГНОСТИКА: 5 ИКОНОК ВМЕСТО 6

## Проблема:
На устройстве Curtis показывается **5 иконок** вместо **6**.

---

## ✅ Что в коде (проверено):

### _screens (6 экранов):
```dart
List<Widget> get _screens => [
  const ChatsTabScreen(),      // 0: Home
  const TeamChatsScreen(),     // 1: Chats
  const VoiceRoomsScreen(),    // 2: Voice
  const ScheduleTabScreen(),   // 3: Schedule
  const EmployeesScreen(),     // 4: Employees ✅
  const ProfileScreen(),       // 5: Profile
];
```

### BottomNavigationBar.items (6 items):
```dart
items: const [
  BottomNavigationBarItem(..., label: 'Home'),      // 0
  BottomNavigationBarItem(..., label: 'Chats'),     // 1
  BottomNavigationBarItem(..., label: 'Voice'),     // 2
  BottomNavigationBarItem(..., label: 'Schedule'),  // 3
  BottomNavigationBarItem(..., label: 'Employees'), // 4 ✅
  BottomNavigationBarItem(..., label: 'Profile'),   // 5
];
```

**Всё правильно! 6 экранов = 6 items**

---

## 🤔 Возможные причины:

### 1. Старая версия на устройстве
**Решение:** Удалить приложение и переустановить

### 2. Кеш Flutter не обновился
**Решение:** flutter clean + пересборка

### 3. UI overflow (экран слишком маленький)
**Решение:** Уменьшить размер иконок или использовать scrollable TabBar

### 4. BottomNavigationBar ограничение
**Проблема:** BottomNavigationBar иногда обрезает items если их >5
**Решение:** Использовать BottomNavigationBarType.fixed (уже установлено)

---

## 🔧 Что я делаю СЕЙЧАС:

1. ✅ Добавил debug логи в код:
   ```dart
   print('🔍 DEBUG: Total screens = 6');
   print('🔍 DEBUG: Building BottomNavigationBar with 6 items');
   ```

2. 🔄 Пересобираю приложение с debug mode:
   ```bash
   flutter run -d Curtis
   ```

3. ⏳ После запуска проверю логи - увидим сколько items реально рендерится

---

## 📱 КАКИЕ 5 ИКОНОК ВЫ ВИДИТЕ?

Напишите мне что показывается на экране, например:
- **Вариант 1:** Home, Chats, Voice, Schedule, Profile (нет Employees)
- **Вариант 2:** Home, Chats, Voice, Schedule, Employees (нет Profile)
- **Вариант 3:** Другая комбинация?

Это поможет понять какая именно иконка пропала!

---

## 🚀 БЫСТРОЕ РЕШЕНИЕ:

### Если Employees точно нет:

1. **Удалите приложение с Curtis:**
   - Долгое нажатие на иконку "Lone Star Chat"
   - Выберите "Удалить приложение"
   - Подтвердите

2. **Установите заново:**
   ```bash
   cd mobile
   flutter clean
   flutter run -d Curtis
   ```

3. **Проверьте результат:**
   - Должно быть 6 иконок: Home, Chats, Voice, Schedule, Employees, Profile

---

## 🔍 DEBUG РЕЖИМ ВКЛЮЧЁН:

Сейчас компилируется новая версия с логами.  
Когда запустится - посмотрим в терминале что пишет:

```
🔍 DEBUG: Total screens = 6
🔍 DEBUG: Building BottomNavigationBar with 6 items
🔍 DEBUG: Items: Home, Chats, Voice, Schedule, Employees, Profile
```

Если увидим это - значит код правильный, проблема в рендеринге.

---

**Статус:** 🔄 Компиляция в процессе  
**ETA:** ~1-2 минуты

После запуска увидим debug логи и поймём где пропала 6-я иконка!
