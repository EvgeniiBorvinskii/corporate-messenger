# 🌐 НАСТРОЙКА DNS ДЛЯ YPILO.COM - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ ЧТО УЖЕ ГОТОВО НА MAC:

- ✅ **Nginx**: Запущен и слушает на порту 80
- ✅ **Backend**: Работает на порту 666
- ✅ **Nginx конфигурация**: Настроена для ypilo.com
- ✅ **Проксирование**: localhost:80 → localhost:666 работает!
- ✅ **Скрипты запуска**: `START_SERVER.sh` и `STOP_SERVER.sh`

**Проверено:**
```
✅ curl http://localhost:80/api/version/check - работает!
✅ Nginx слушает на *:80 (все интерфейсы)
✅ Backend слушает на *:666 (все интерфейсы)
```

---

## 📊 ВАШИ СЕТЕВЫЕ ДАННЫЕ:

- **Публичный IP:** `165.140.231.35`
- **Локальный IP Mac:** `192.168.28.17`
- **Домен:** `ypilo.com`

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ (только DNS):

### Шаг 1: Найдите где зарегистрирован домен ypilo.com

Проверьте у какого регистратора ваш домен:
```bash
whois ypilo.com
```

Популярные регистраторы:
- **GoDaddy** (godaddy.com)
- **Namecheap** (namecheap.com)
- **Cloudflare** (cloudflare.com)
- **Google Domains** (domains.google.com)
- **REG.RU** (reg.ru)
- **Другие**

---

### Шаг 2: Зайдите в панель управления доменом

#### Вариант A: GoDaddy
1. Зайдите на **godaddy.com**
2. Войдите в свой аккаунт
3. **My Products** → Найдите **ypilo.com**
4. Нажмите **DNS** или **Manage DNS**

#### Вариант B: Namecheap
1. Зайдите на **namecheap.com**
2. Войдите в свой аккаунт
3. **Domain List** → Найдите **ypilo.com**
4. Нажмите **Manage** → **Advanced DNS**

#### Вариант C: Cloudflare
1. Зайдите на **cloudflare.com**
2. Войдите в свой аккаунт
3. Выберите **ypilo.com** из списка доменов
4. Перейдите на вкладку **DNS**

#### Вариант D: REG.RU
1. Зайдите на **reg.ru**
2. Войдите в свой аккаунт
3. **Домены** → Найдите **ypilo.com**
4. **Управление доменом** → **Управление DNS**

---

### Шаг 3: Создайте A-запись

В разделе DNS найдите кнопку **Add Record** или **Добавить запись**.

**Создайте новую A-запись со следующими параметрами:**

```
┌─────────────────────────────────────────────────┐
│ Type:    A                                      │
│ Name:    @  (или оставьте пустым, или ypilo.com)│
│ Value:   165.140.231.35                         │
│ TTL:     3600 (или Auto/Default)                │
└─────────────────────────────────────────────────┘
```

**Пояснения:**
- **Type: A** - это тип записи (Address Record - указывает IP)
- **Name: @** - символ @ означает корневой домен (ypilo.com)
- **Value: 165.140.231.35** - ваш публичный IP адрес
- **TTL: 3600** - время кеширования (1 час)

**Скриншот для примера (GoDaddy):**
```
Type    Name    Value               TTL
A       @       165.140.231.35      1 Hour
```

---

### Шаг 4: (Опционально) Создайте запись для www

Если хотите чтобы работал и **www.ypilo.com**, создайте ещё одну запись:

```
┌─────────────────────────────────────────────────┐
│ Type:    A                                      │
│ Name:    www                                    │
│ Value:   165.140.231.35                         │
│ TTL:     3600                                   │
└─────────────────────────────────────────────────┘
```

Или создайте CNAME:
```
┌─────────────────────────────────────────────────┐
│ Type:    CNAME                                  │
│ Name:    www                                    │
│ Value:   ypilo.com                              │
│ TTL:     3600                                   │
└─────────────────────────────────────────────────┘
```

---

### Шаг 5: Сохраните изменения

1. Нажмите **Save** или **Сохранить**
2. Подтвердите изменения если потребуется

**DNS изменения распространяются от 5 минут до 48 часов** (обычно 10-30 минут).

---

## 🔍 ПРОВЕРКА DNS:

### Через 5-10 минут проверьте:

```bash
# На Mac:
nslookup ypilo.com

# Должен вернуть:
# Server:  192.168.28.1
# Address: 192.168.28.1#53
#
# Non-authoritative answer:
# Name:    ypilo.com
# Address: 165.140.231.35
```

Или используйте онлайн-инструменты:
- https://dnschecker.org
- https://www.whatsmydns.net

Введите **ypilo.com** и проверьте что A-запись показывает **165.140.231.35**.

---

## 🚧 НАСТРОЙКА PORT FORWARDING НА РОУТЕРЕ:

**Это нужно чтобы запросы из интернета на порт 80 перенаправлялись на ваш Mac.**

### Шаг 1: Найдите IP роутера

Обычно: `192.168.1.1` или `192.168.0.1`

Или посмотрите:
```bash
netstat -nr | grep default
```

### Шаг 2: Зайдите в настройки роутера

1. Откройте браузер
2. Введите IP роутера (например `http://192.168.1.1`)
3. Войдите (логин/пароль обычно на наклейке роутера или `admin`/`admin`)

### Шаг 3: Найдите раздел Port Forwarding

Ищите:
- **Port Forwarding**
- **Virtual Server**
- **NAT Forwarding**
- **Переадресация портов**
- **Applications & Gaming** (у некоторых роутеров)

### Шаг 4: Создайте правило

```
┌──────────────────────────────────────────────────┐
│ Service Name:     Lone Star Chat                │
│ External Port:    80                             │
│ Internal IP:      192.168.28.17                  │
│ Internal Port:    80                             │
│ Protocol:         TCP                            │
│ Enabled:          Yes / On                       │
└──────────────────────────────────────────────────┘
```

**Пояснения:**
- **External Port: 80** - запросы из интернета на порт 80
- **Internal IP: 192.168.28.17** - IP вашего Mac
- **Internal Port: 80** - порт на Mac (где слушает Nginx)

### Шаг 5: Сохраните и примените

Нажмите **Save** / **Apply** / **Применить**.

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА:

### После настройки DNS и Port Forwarding:

#### 1. С вашего Mac:
```bash
curl http://ypilo.com/api/version/check
```
Должен вернуть JSON с версией.

#### 2. С iPhone на мобильном интернете:
Откройте Safari и зайдите:
```
http://ypilo.com/api/version/check
```
Должен показать JSON.

#### 3. Из приложения:
1. Откройте приложение на iPhone (на мобильном интернете)
2. Зайдите в настройки (⚙️)
3. Посмотрите текущий IP
4. Если показывает `192.168.28.17` - приложение попробует автоматически переключиться
5. Попробуйте авторизоваться

**Приложение автоматически найдёт рабочий IP!**

---

## 🎯 КАК РАБОТАЕТ АВТОПЕРЕКЛЮЧЕНИЕ:

### Сценарий 1: iPhone на WiFi (дома)
```
iPhone → WiFi → Mac (192.168.28.17:666) → Backend
✅ Быстро, локально
```

### Сценарий 2: iPhone на мобильном интернете
```
iPhone → Internet → ypilo.com:80 →
→ Роутер (165.140.231.35:80) →
→ Mac (192.168.28.17:80) →
→ Nginx (localhost:80) →
→ Backend (localhost:666)
✅ Работает из любой точки мира!
```

**Приложение пытается подключиться в таком порядке:**
1. `192.168.28.17:666` (локальный WiFi)
2. `172.20.10.5:666` (Personal Hotspot)
3. `ypilo.com:80` (публичный домен)

И **автоматически** выбирает первый работающий!

---

## 📋 КОНТРОЛЬНЫЙ СПИСОК:

### На Mac (уже готово ✅):
- [x] Nginx установлен и запущен
- [x] Nginx слушает на порту 80
- [x] Backend запущен на порту 666
- [x] Nginx проксирует ypilo.com → localhost:666
- [x] Тест localhost:80 работает
- [x] Публичный IP известен: 165.140.231.35

### Нужно сделать вручную:
- [ ] **DNS A-запись**: ypilo.com → 165.140.231.35
- [ ] **Port Forwarding**: 80 → 192.168.28.17:80
- [ ] **Проверка DNS**: `nslookup ypilo.com`
- [ ] **Проверка доступа**: `curl http://ypilo.com`

---

## 🚀 БЫСТРЫЙ СТАРТ:

### Запустить сервер:
```bash
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat"
./START_SERVER.sh
```

### Остановить сервер:
```bash
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat"
./STOP_SERVER.sh
```

### Посмотреть логи Backend:
```bash
tail -f /tmp/backend.log
```

### Посмотреть логи Nginx:
```bash
tail -f /opt/homebrew/var/log/nginx/lonestar_error.log
```

---

## ❓ ЧАСТЫЕ ВОПРОСЫ:

### Q: Как узнать что DNS обновился?
A: Выполните `nslookup ypilo.com` - если показывает 165.140.231.35, значит обновился.

### Q: DNS обновился, но всё равно не работает?
A: Проверьте Port Forwarding на роутере. Тест: `curl http://165.140.231.35` с внешнего устройства.

### Q: Мой IP может измениться?
A: Да, если у вас динамический IP от провайдера. Решения:
   - Использовать сервис Dynamic DNS (DDNS)
   - Заказать статический IP у провайдера
   - Использовать ngrok как временное решение

### Q: Приложение не переключается на ypilo.com?
A: Удалите приложение с iPhone и установите заново через Xcode - сбросятся настройки IP.

---

## 💡 ЕСЛИ DNS ДОЛГО НЕ РАБОТАЕТ:

### Временное решение: ngrok
```bash
# Установить ngrok
brew install ngrok

# Запустить туннель
ngrok http 666

# Скопировать URL (например https://abc123.ngrok-free.app)
# Ввести этот URL в настройках приложения
```

**Плюсы:** Работает сразу, без настройки DNS/роутера  
**Минусы:** URL меняется при перезапуске (бесплатная версия)

---

## 🎉 ПОСЛЕ НАСТРОЙКИ:

Когда DNS и Port Forwarding настроены, приложение будет:
- ✅ Работать на WiFi (через 192.168.28.17)
- ✅ Работать на мобильном интернете (через ypilo.com)
- ✅ Автоматически переключаться между ними
- ✅ Работать из любой точки мира!

**Нужна помощь - спрашивайте!** 🚀
