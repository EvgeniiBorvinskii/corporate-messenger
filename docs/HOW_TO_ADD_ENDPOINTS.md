# 🚀 КАК ДОБАВИТЬ НЕДОСТАЮЩИЕ API ЭНДПОИНТЫ

## Дата: 9 декабря 2025

---

## 📦 ФАЙЛЫ ДЛЯ ЗАГРУЗКИ НА СЕРВЕР

Я создал скрипт `add_missing_endpoints.js` который автоматически добавит все 5 недостающих эндпоинтов в ваш backend.

---

## 🔧 ИНСТРУКЦИЯ ПО УСТАНОВКЕ

### Шаг 1: Загрузите скрипт на сервер

```bash
# С вашего Mac
scp add_missing_endpoints.js root@5.249.160.54:/root/
```

**Или** просто скопируйте содержимое файла `add_missing_endpoints.js` и создайте его на сервере вручную.

---

### Шаг 2: Подключитесь к серверу

```bash
ssh root@5.249.160.54
# Пароль: 54654250pS3123
```

---

### Шаг 3: Найдите главный файл сервера

```bash
# Попробуйте найти server-chat.js или похожий файл
find /opt /srv /root -name "*server*.js" -type f 2>/dev/null

# Обычно он находится в одном из этих мест:
# /opt/lone-star-chat/backend/server-chat.js
# /opt/lone-star-chat/backend/server-chat-current.js
# /srv/Lone Star Chat/backend/server.js
```

**Запомните путь к файлу!**

---

### Шаг 4: Запустите скрипт

```bash
# Замените /path/to/server-chat.js на реальный путь
node /root/add_missing_endpoints.js /opt/lone-star-chat/backend/server-chat.js
```

**Что произойдет:**
- ✅ Скрипт создаст резервную копию файла (на всякий случай)
- ✅ Добавит 5 новых эндпоинтов в код сервера
- ✅ Выведет список добавленных эндпоинтов

---

### Шаг 5: Перезапустите сервер

```bash
# Если используется systemd
systemctl restart lone-star-chat

# ИЛИ если используется PM2
pm2 restart lone-star-chat

# ИЛИ если запущен вручную
pkill -f "node.*server"
cd /opt/lone-star-chat/backend
node server-chat.js &
```

---

### Шаг 6: Проверьте что все работает

```bash
# Получите токен (замените на реальный email/password)
TOKEN=$(curl -s -X POST http://localhost:80/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@lonestar.com","password":"your_password"}' \
  | grep -o '"token":"[^"]*"' | cut -d'"' -f4)

echo "Token: $TOKEN"

# 1. Проверьте эндпоинт обновления ролей
curl -X PUT http://localhost:80/api/admin/employees/2/roles \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"roles": ["sales"]}'

# Ожидаемый ответ: {"message":"Roles updated successfully",...}

# 2. Проверьте эндпоинт DM чатов
curl -X GET http://localhost:80/api/messages/dm/2/messages \
  -H "Authorization: Bearer $TOKEN"

# Ожидаемый ответ: {"messages":[...]}
```

---

## ✅ КАКИЕ ЭНДПОИНТЫ БУДУТ ДОБАВЛЕНЫ

1. **PUT /api/admin/employees/:id/roles** - Обновление ролей работника
2. **DELETE /api/admin/employees/:id** - Удаление работника
3. **POST /api/admin/employees** - Создание нового работника
4. **GET /api/messages/dm/:userId/messages** - Получение DM сообщений
5. **POST /api/messages/dm/:userId/messages** - Отправка DM сообщения

---

## ⚠️ ВАЖНО

- Скрипт автоматически создает **резервную копию** вашего файла сервера
- Если что-то пойдет не так, вы всегда сможете восстановить из копии
- Файл копии будет называться `server-chat.js.backup.TIMESTAMP`

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Восстановление из резервной копии:

```bash
# Найдите файл копии
ls -lah /opt/lone-star-chat/backend/*.backup.*

# Восстановите из копии (замените TIMESTAMP на реальное значение)
cp /opt/lone-star-chat/backend/server-chat.js.backup.TIMESTAMP \
   /opt/lone-star-chat/backend/server-chat.js

# Перезапустите сервер
systemctl restart lone-star-chat
```

---

## 📱 ПОСЛЕ УСТАНОВКИ

После успешного добавления эндпоинтов и перезапуска сервера:

1. **Откройте мобильное приложение**
2. **Протестируйте:**
   - Назначение ролей работникам
   - Удаление работников
   - Создание новых работников
   - Отправку DM сообщений между пользователями

**Все должно заработать!** 🎉

---

## 🤔 НУЖНА ПОМОЩЬ?

Если возникли проблемы:
1. Проверьте логи сервера: `journalctl -u lone-star-chat -f`
2. Проверьте что сервер запущен: `ps aux | grep node`
3. Напишите мне и пришлите ошибки из логов
