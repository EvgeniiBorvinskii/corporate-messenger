# 🌐 YPILO.COM - ПУБЛИЧНЫЙ ДОСТУП НАСТРОЕН!

**Дата:** 10 ноября 2025  
**Версия:** Public Domain Edition

---

## ✅ ЧТО НАСТРОЕНО:

### 1. **Nginx Reverse Proxy**

Nginx проксирует запросы с **ypilo.com** на ваш Node.js сервер (порт 666):

```
Интернет → ypilo.com:80 → Nginx → localhost:666 → Node.js
```

**Конфигурация:** `/opt/homebrew/etc/nginx/servers/ypilo.com.conf`

```nginx
server {
    listen 80;
    server_name ypilo.com www.ypilo.com;
    
    location / {
        proxy_pass http://127.0.0.1:666;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        # ... CORS headers, timeouts
    }
}
```

---

### 2. **Приложение обновлено**

Теперь использует **ypilo.com** как основной сервер:

```dart
static const List<String> suggestedIPs = [
  'ypilo.com',      // ✅ ОСНОВНОЙ - публичный домен!
  '192.168.28.17',  // Локальный Wi-Fi (резерв)
  '172.20.10.5',    // Мобильный hotspot (резерв)
  // ...
];
```

**Умная логика портов:**
- `ypilo.com` → порт **80** (Nginx)
- Локальные IP → порт **666** (прямое подключение)

---

### 3. **DNS настройки для ypilo.com**

⚠️ **ВАЖНО!** Убедитесь что DNS запись настроена:

```
Тип: A
Имя: @
Значение: [ВНЕШНИЙ IP ВАШЕГО СЕРВЕРА]
TTL: 3600
```

Для проверки:
```bash
# Узнать внешний IP Mac:
curl ifconfig.me

# Проверить DNS:
nslookup ypilo.com
# Должен показать ваш внешний IP
```

---

## 🚀 КАК ЭТО РАБОТАЕТ:

### Архитектура:

```
iPhone (мобильный интернет)
    ↓
    Internet
    ↓
ypilo.com:80 (DNS → ваш внешний IP)
    ↓
Nginx (порт 80)
    ↓
Proxy → localhost:666
    ↓
Node.js (Lone Star Chat)
```

### Для локальной разработки:

```
iPhone (домашний Wi-Fi)
    ↓
192.168.28.17:666 (прямое подключение)
    ↓
Node.js (без Nginx)
```

---

## 📊 СТАТУС СЕРВИСОВ:

### Nginx:
```bash
# Статус:
brew services list | grep nginx
# Результат: nginx started

# Логи:
tail -f /opt/homebrew/var/log/nginx/lonestar_access.log
tail -f /opt/homebrew/var/log/nginx/lonestar_error.log
```

### Backend (Node.js):
```bash
# Статус:
ps aux | grep "node.*server-chat-current" | grep -v grep
# Результат: PID 2935

# Порт:
lsof -i :666
# Результат: node 2935 ... *:mdqs (LISTEN)
```

---

## ✅ ПРОВЕРКА:

### Тест 1: Локальный доступ через Nginx
```bash
curl http://localhost:80/api/version/check
# Должен вернуть: {"supported":true,...}
```

### Тест 2: Через домен (если DNS настроен)
```bash
curl http://ypilo.com/api/version/check
# Должен вернуть: {"supported":true,...}
```

### Тест 3: Авторизация через домен
```bash
curl -X POST http://ypilo.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin","password":"admin"}'
# Должен вернуть токен
```

---

## 🔧 НАСТРОЙКИ DNS (ЕСЛИ ЕЩЁ НЕ СДЕЛАНО):

### Шаг 1: Узнать внешний IP Mac
```bash
curl ifconfig.me
# Например: 85.123.45.67
```

### Шаг 2: Настроить A-запись у регистратора ypilo.com

В панели управления доменом:

```
Тип:    A
Хост:   @           (или ypilo.com)
Значение: 85.123.45.67  (ваш внешний IP)
TTL:    3600
```

Для www поддомена:

```
Тип:    CNAME
Хост:   www
Значение: ypilo.com
TTL:    3600
```

### Шаг 3: Проброс порта на роутере

Зайдите в настройки роутера и настройте Port Forwarding:

```
Внешний порт: 80
Внутренний IP: 192.168.28.17
Внутренний порт: 80
Протокол: TCP
```

---

## 📱 ИСПОЛЬЗОВАНИЕ В ПРИЛОЖЕНИИ:

### После установки новой версии:

1. Откройте приложение
2. При первом запуске будет использован **ypilo.com**
3. Автоматически подключится через порт 80
4. Работает с мобильным интернетом! ✅

### Fallback на локальные IP:

Если **ypilo.com** недоступен:
- Приложение автоматически попробует `192.168.28.17:666`
- Затем `172.20.10.5:666`
- И так далее по списку suggestedIPs

---

## 🎯 РЕЗУЛЬТАТ:

### ТЕПЕРЬ РАБОТАЕТ:

```
✅ Мобильный интернет → ypilo.com:80 → Nginx → Node.js
✅ Домашний Wi-Fi → 192.168.28.17:666 → Node.js (прямо)
✅ Другой Wi-Fi → ypilo.com:80 → Nginx → Node.js
✅ VPN → ypilo.com:80 → Nginx → Node.js
✅ Любое подключение! 🎉
```

---

## 📝 КОМАНДЫ ДЛЯ УПРАВЛЕНИЯ:

### Nginx:
```bash
# Запуск:
brew services start nginx

# Остановка:
brew services stop nginx

# Перезапуск:
brew services restart nginx

# Проверка конфигурации:
nginx -t

# Просмотр логов:
tail -f /opt/homebrew/var/log/nginx/lonestar_access.log
```

### Backend:
```bash
# Запуск:
cd backend && PORT=666 node server-chat-current.js &

# Остановка:
pkill -f "node.*server-chat-current"

# Просмотр логов:
tail -f /tmp/backend.log
```

---

## ⚠️ ВАЖНО - УСТАНОВИТЕ ПРИЛОЖЕНИЕ!

**Новая версия собрана:** `Runner.app (57.8 MB)`

**НО НЕ УСТАНОВЛЕНА на iPhone!**

### Установка:

```bash
# 1. Откройте Xcode:
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat"
./OPEN_XCODE.sh

# 2. Подключите iPhone через USB
# 3. В Xcode: выберите iPhone
# 4. Нажмите ▶️ (Play)
# 5. Подождите 2 минуты
```

---

## 🎉 ФИНАЛЬНАЯ ПРОВЕРКА:

### После установки приложения:

1. **Переключитесь на мобильный интернет (4G/5G)**
2. Откройте приложение
3. Войдите: `admin` / `admin`
4. ✅ Должно работать через **ypilo.com**!

---

## 📄 ФАЙЛЫ:

- **Nginx config:** `/opt/homebrew/etc/nginx/servers/ypilo.com.conf`
- **App config:** `mobile/lib/core/config/api_config.dart`
- **Backend:** `backend/server-chat-current.js`

---

**🌐 ПУБЛИЧНЫЙ ДОСТУП ГОТОВ! УСТАНОВИТЕ ПРИЛОЖЕНИЕ!** 🚀
