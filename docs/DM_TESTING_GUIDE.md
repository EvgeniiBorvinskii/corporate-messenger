# 🧪 Тестирование DM Чатов - Быстрый Гайд

## ✅ Перед тестированием

### 1. Проверь компиляцию:
```bash
cd /Users/svetanaborvinskaia/Desktop/Lone\ Star\ Chat/mobile
flutter analyze
```
**Ожидаемый результат:** `No issues found!`

### 2. Запусти backend:
```bash
cd /Users/svetanaborvinskaia/Desktop/Lone\ Star\ Chat/backend
node server-chat-current.js
```
**Ожидаемый результат:** `Server running on http://0.0.0.0:3000`

### 3. Запусти приложение:
```bash
cd /Users/svetanaborvinskaia/Desktop/Lone\ Star\ Chat/mobile
flutter run -d Curtis
```

---

## 📝 Тестовый сценарий #1: Отправить DM

### Шаги:
1. ✅ Открой приложение
2. ✅ Войди в аккаунт (admin / admin123)
3. ✅ Нажми на таб **"Employees"** (5-я кнопка снизу)
4. ✅ Найди любого сотрудника (например, "Svetlana Borvinskaia")
5. ✅ Нажми на карточку сотрудника
6. ✅ В модальном окне нажми кнопку **"Написать"** (синяя кнопка)
7. ✅ Должен открыться экран чата с сотрудником
8. ✅ Напиши сообщение: "Привет! Это тест DM чата"
9. ✅ Нажми кнопку Send (стрелка вправо)
10. ✅ Сообщение должно появиться в чате справа (от тебя)

### Ожидаемый результат:
- Чат открылся без ошибок
- Аватар сотрудника отображается в AppBar
- Сообщение появилось в списке
- Timestamp показывается (HH:mm)
- Input field очистился после отправки
- Скролл автоматически прокрутился вниз

---

## 📝 Тестовый сценарий #2: Проверить Persistence

### Шаги:
1. ✅ Отправь 2-3 сообщения в DM чат
2. ✅ Закрой приложение
3. ✅ Останови backend (`Ctrl+C` в терминале)
4. ✅ Проверь создался ли файл:
```bash
cat backend/database/dm_messages.json
```
5. ✅ Должен увидеть JSON с твоими сообщениями
6. ✅ Запусти backend снова:
```bash
node backend/server-chat-current.js
```
7. ✅ Запусти приложение снова
8. ✅ Открой тот же DM чат (Employees → сотрудник → Написать)
9. ✅ Все предыдущие сообщения должны загрузиться!

### Ожидаемый результат:
- `dm_messages.json` создан
- Сообщения сохранены в правильном формате
- После перезапуска история не потеряна
- Все сообщения загружаются корректно

---

## 📝 Тестовый сценарий #3: Bidirectional Chat

### Подготовка:
Нужно 2 пользователя для теста. Создадим второго:
```bash
# В backend/database/users.json добавь второго пользователя
# Или используй существующих: admin (id: "1") и svet (id: "2")
```

### Шаги:
1. ✅ Войди как **admin** (id: "1")
2. ✅ Открой DM чат с **svet** (id: "2")
3. ✅ Отправь сообщение: "Привет от админа!"
4. ✅ Проверь `dm_messages.json`:
```bash
cat backend/database/dm_messages.json | grep "1_2"
```
5. ✅ Conversation ID должен быть `"1_2"` (sorted)
6. ✅ Выйди из аккаунта
7. ✅ Войди как **svet** (id: "2")
8. ✅ Открой DM чат с **admin** (id: "1")
9. ✅ Должен увидеть сообщение "Привет от админа!"
10. ✅ Отправь ответ: "Привет! Получил твое сообщение"
11. ✅ Проверь `dm_messages.json` снова:
```bash
cat backend/database/dm_messages.json
```
12. ✅ Оба сообщения должны быть в одном массиве под `"1_2"`

### Ожидаемый результат:
- Conversation ID один и тот же для обоих: `"1_2"`
- Оба пользователя видят одну и ту же историю
- Сообщения упорядочены по времени
- Avatars обоих пользователей отображаются

---

## 📝 Тестовый сценарий #4: Множественные чаты

### Шаги:
1. ✅ Войди как **admin** (id: "1")
2. ✅ Открой DM с **svet** (id: "2")
3. ✅ Отправь: "Сообщение для Светы"
4. ✅ Вернись в Employees
5. ✅ Открой DM с другим сотрудником (id: "3")
6. ✅ Отправь: "Сообщение для другого сотрудника"
7. ✅ Проверь `dm_messages.json`:
```bash
cat backend/database/dm_messages.json
```
8. ✅ Должно быть 2 ключа: `"1_2"` и `"1_3"`
9. ✅ Каждый содержит свою историю

### Ожидаемый результат:
- Разные переписки не смешиваются
- Каждый conversation ID уникален
- Каждая история сохраняется отдельно
- Переключение между чатами работает

---

## 📝 Тестовый сценарий #5: UI Elements

### Проверь визуальные элементы:

#### AppBar:
- ✅ Аватар собеседника отображается (круглый, radius 20)
- ✅ Имя собеседника в центре
- ✅ Online status (зеленая точка если online)
- ✅ Кнопка "Назад" слева

#### Message List:
- ✅ Свои сообщения справа (синий оттенок)
- ✅ Чужие сообщения слева (серый оттенок)
- ✅ Liquid Glass эффект (5% white, 15% border)
- ✅ Border radius 16px
- ✅ Timestamps внизу каждого сообщения (HH:mm)
- ✅ Аватарки рядом с сообщениями (если есть)

#### Input Area:
- ✅ TextField с placeholder "Введите сообщение..."
- ✅ Кнопка Send справа (иконка стрелки)
- ✅ Кнопка disabled во время отправки
- ✅ Liquid Glass фон

#### Loading States:
- ✅ CircularProgressIndicator при загрузке
- ✅ "Нет сообщений" когда пусто
- ✅ Smooth transitions

---

## 🐛 Возможные проблемы и решения

### Проблема: Import error в employees_screen.dart
```
Error: Target of URI doesn't exist: '../../dm/dm_chat_screen.dart'
```
**Решение:**
```bash
# Проверь, что файл существует:
ls mobile/lib/screens/dm/dm_chat_screen.dart

# Если нет, создай папку:
mkdir -p mobile/lib/screens/dm
```

### Проблема: Backend не создает dm_messages.json
```
ENOENT: no such file or directory
```
**Решение:**
```bash
# Создай папку вручную:
cd backend
mkdir -p database
```

### Проблема: Аватарки не загружаются
```
404 Not Found
```
**Решение:**
```bash
# Проверь URL в консоли:
# Должен быть: http://5.249.160.54:3002/uploads/avatars/...

# Проверь, что папка существует:
ls backend/uploads/avatars
```

### Проблема: Conversation ID дублируется
```
Два ключа: "1_2" и "2_1"
```
**Решение:**
Это баг! Conversation ID должен быть sorted. Проверь `getDMConversationId()`:
```javascript
function getDMConversationId(userId1, userId2) {
  return [userId1, userId2].sort().join('_'); // ✅ Должен быть .sort()
}
```

---

## ✅ Финальный чек-лист

### Backend:
- [ ] Server запущен на порту 3000
- [ ] `dm_messages.json` создается автоматически
- [ ] Сообщения сохраняются после каждой отправки
- [ ] Conversation ID всегда sorted
- [ ] API возвращает полные user объекты

### Frontend:
- [ ] `flutter analyze` без ошибок
- [ ] DMChatScreen компилируется
- [ ] Кнопка "Написать" работает
- [ ] Чат открывается без задержек
- [ ] Сообщения отправляются
- [ ] Timestamps форматируются
- [ ] Аватарки отображаются
- [ ] Auto-scroll работает
- [ ] Loading states корректны

### Persistence:
- [ ] Сообщения не теряются при перезапуске backend
- [ ] Сообщения не теряются при закрытии приложения
- [ ] История загружается при открытии чата
- [ ] Множественные чаты не смешиваются

---

## 📊 Метрики успеха

### Performance:
- ⏱️ Отправка сообщения: < 1 секунда
- ⏱️ Загрузка истории: < 2 секунд
- ⏱️ Открытие чата: < 0.5 секунд

### Reliability:
- 💾 100% persistence (все сообщения сохраняются)
- 🔄 0 потерянных сообщений при перезапуске
- ✅ 0 ошибок компиляции

### UX:
- 🎨 Liquid Glass UI соблюден
- ⚡ Smooth animations
- 📱 Responsive touch feedback
- 🔄 Auto-scroll работает

---

## 🎉 Если все тесты прошли

**Поздравляю! DM Chats система полностью работает!** 🎊

Теперь можешь:
1. Показать заказчику
2. Тестировать с реальными пользователями
3. Добавить дополнительные функции (image attachments, read status, etc.)

---

## 📞 Следующий шаг (опционально)

Если нужны голосовые звонки, начни с:
1. Изучить WebRTC (flutter_webrtc library)
2. Создать signaling server
3. Реализовать CallScreen UI
4. Добавить mic permissions

**Estimated time:** 8-12 hours

---

**Дата создания:** 10 января 2025  
**Версия:** 1.0  
**Статус:** ✅ Ready for testing
