# 📋 Инструкция по обновлению сервера 5.249.160.54:666

## Дата: 12 декабря 2025

---

## Вариант 1: Автоматическая загрузка (если есть SSH)

### Шаг 1: Настроить скрипт

Откройте файл `upload-to-server.sh` и проверьте/измените:

```bash
SERVER_IP="5.249.160.54"
SERVER_USER="root"  # ← ИЗМЕНИТЕ на своего пользователя!
SERVER_PATH="/var/www/chat-backend"  # ← ИЗМЕНИТЕ на путь к backend!
```

### Шаг 2: Запустить скрипт

```bash
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat"
./upload-to-server.sh
```

Скрипт:
1. ✅ Создаст backup старого файла
2. ✅ Загрузит новый `server-chat.js`
3. ✅ Предложит перезапустить сервер
4. ✅ Проверит что всё работает

---

## Вариант 2: Ручная загрузка через SCP

```bash
# 1. Загрузить файл
scp "/Users/svetanaborvinskaia/Desktop/Lone Star Chat/backend/server-chat.js" \
    root@5.249.160.54:/var/www/chat-backend/

# 2. Подключиться к серверу
ssh root@5.249.160.54

# 3. На сервере: создать backup
cd /var/www/chat-backend
cp server-chat.js server-chat.js.backup

# 4. Перезапустить (выберите один метод):

# Если PM2:
pm2 restart chat-server

# Если systemd:
sudo systemctl restart chat-server

# Если вручную:
killall node
nohup node server-chat.js > server.log 2>&1 &

# 5. Проверить логи
pm2 logs chat-server
# или
tail -f server.log
```

---

## Вариант 3: Через панель управления хостингом

Если у вас есть веб-интерфейс (cPanel, Plesk, etc.):

1. Войти в панель управления
2. Файловый менеджер → найти папку backend
3. Создать backup `server-chat.js` → `server-chat.js.backup`
4. Загрузить новый файл из:
   `/Users/svetanaborvinskaia/Desktop/Lone Star Chat/backend/server-chat.js`
5. Перезапустить Node.js процесс через панель

---

## Вариант 4: Попросить администратора сервера

Если нет доступа к серверу:

**Отправить администратору**:

```
Привет! Нужно обновить файл server-chat.js на сервере 5.249.160.54:666

Что нужно сделать:
1. Создать backup текущего файла
2. Заменить server-chat.js новым (прикрепляю файл)
3. Перезапустить Node.js сервис
4. Проверить что Socket.io работает:
   curl http://5.249.160.54:666/socket.io/
   (не должно быть 404)

Спасибо!
```

**Файл для отправки**:
`/Users/svetanaborvinskaia/Desktop/Lone Star Chat/backend/server-chat.js`

---

## Проверка после обновления:

### 1. Проверить HTTP
```bash
curl http://5.249.160.54:666/
# Должна вернуться HTML страница
```

### 2. Проверить Socket.io
```bash
curl http://5.249.160.54:666/socket.io/
# НЕ должно быть 404!
# Должно быть что-то типа:
# {"code":0,"message":"Transport unknown"}
```

### 3. Проверить в приложении
1. Запустить приложение на iPhone
2. Войти в аккаунт
3. Проверить логи - НЕ должно быть:
   ```
   ❌ WebSocket error: ... HTTP status code: 404
   ```
4. Должно быть:
   ```
   ✅ 🔌 WebSocket connected via PresenceProvider
   ✅ 🔌 WebRTC Socket connected
   ```

### 4. Попробовать звонок
1. Открыть профиль пользователя
2. Нажать кнопку звонка
3. Приложение НЕ должно крашиться
4. Должен появиться экран звонка

---

## Что изменилось в server-chat.js:

### Добавлены Socket.io события:
- `call:initiate` - инициация звонка
- `call:incoming` - входящий звонок
- `call:accepted` - звонок принят
- `call:rejected` - звонок отклонён
- `call:ringing` - идёт вызов
- `call:offer` - WebRTC SDP offer
- `call:answer` - WebRTC SDP answer
- `call:ice-candidate` - ICE кандидат
- `call:end` - завершение звонка
- `call:toggle-audio` - переключение микрофона
- `call:toggle-video` - переключение камеры

### Добавлены API endpoints:
- `GET /api/calls/history` - история звонков
- `GET /api/calls/history/:userId` - история с пользователем
- `DELETE /api/calls/history/:callId` - удаление записи

### Добавлено хранилище:
- `const callHistory = []` - массив записей звонков

---

## Если что-то пошло не так:

### Откатить изменения:
```bash
ssh root@5.249.160.54
cd /var/www/chat-backend
cp server-chat.js.backup server-chat.js
pm2 restart chat-server
```

### Проверить логи ошибок:
```bash
# PM2
pm2 logs chat-server --err

# Systemd
sudo journalctl -u chat-server -n 50

# Файл
tail -100 server.log
```

### Типичные проблемы:

**1. "Permission denied"**
```bash
# Дать права на запись
chmod 644 server-chat.js
chown www-data:www-data server-chat.js
```

**2. "Port 666 already in use"**
```bash
# Найти процесс
lsof -ti :666
# Убить старый процесс
kill -9 $(lsof -ti :666)
# Запустить заново
node server-chat.js
```

**3. "Module not found"**
```bash
# Установить зависимости
cd /var/www/chat-backend
npm install
```

---

## ✅ После успешного обновления:

1. Обновить TODO:
   - ✅ Upload backend code to dedicated server
   
2. Пересобрать приложение:
   ```bash
   cd mobile
   flutter clean
   flutter pub get
   flutter build ios --release --no-codesign
   ```

3. Протестировать на устройстве:
   - ✅ WebSocket подключается
   - ✅ Звонки не крашат приложение
   - ✅ Микрофон/камера запрашивают разрешение
   - ✅ Блюр на весь экран
   - ✅ Быстрый логин

---

**Удачи! 🚀**
