# 🚀 Напоминание о выделенном сервере

## ✅ Используется ВЫДЕЛЕННЫЙ СЕРВЕР

**IP**: `5.249.160.54`  
**Порт**: `666`  
**URL**: `http://5.249.160.54:666`

## ⚠️ ВАЖНО

1. **НЕ запускайте локальный сервер** (`node server-chat.js`)
2. Все изменения должны быть на выделенном сервере
3. Приложение подключается к `5.249.160.54:666` автоматически

## 📱 Конфигурация в приложении

Файл: `mobile/lib/core/config/api_config.dart`

```dart
static const List<String> suggestedIPs = [
  '5.249.160.54',    // ✅ PRIMARY - Выделенный сервер
  // ...
];

static String getPortForHost(String host) {
  if (host == '5.249.160.54') {
    return '666';  // ✅ Правильный порт
  }
  // ...
}
```

## 🔍 Проверка сервера

```bash
# Проверка что сервер работает
curl http://5.249.160.54:666/api/channels
# Ответ: {"error":"Authentication required"} ✅

# Проверка WebSocket
curl http://5.249.160.54:666/
# Ответ: HTML страница ✅
```

## 📝 Обновление кода на сервере

Если нужно обновить код на выделенном сервере:

```bash
# 1. Подключиться к серверу по SSH
ssh user@5.249.160.54

# 2. Перейти в папку проекта
cd /path/to/backend

# 3. Обновить файлы (scp, git, etc.)

# 4. Перезапустить сервер
pm2 restart chat-server
# или
systemctl restart chat-server
```

## 🎯 Текущий статус

- ✅ Выделенный сервер работает на `5.249.160.54:666`
- ✅ Приложение настроено на подключение к выделенному серверу
- ✅ Локальный сервер НЕ используется
- ✅ Все исправления звонков готовы для тестирования

## 📞 WebRTC на выделенном сервере

Все события WebRTC уже реализованы:
- `call:initiate`
- `call:incoming`
- `call:accepted`
- `call:rejected`
- `call:offer`
- `call:answer`
- `call:ice-candidate`
- `call:end`
- `call:toggle-audio`
- `call:toggle-video`

**Код обновлён в приложении, готов работать с выделенным сервером!**

---

**Дата**: 11 декабря 2025  
**Сервер**: 5.249.160.54:666  
**Статус**: ✅ Работает
