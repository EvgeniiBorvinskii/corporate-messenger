# 📱 Полная переустановка приложения на iPhone

## Проблема
После обновления кода и пересборки приложение всё еще работает со старым кодом.

## Причина
Flutter может кэшировать Provider'ы и не обновлять их при обычной пересборке.

## Решение: Полная переустановка

### Вариант 1: Через iPhone (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Удалите приложение
1. Найдите **Lone Star Chat** на главном экране iPhone
2. **Долгое нажатие** на иконку приложения
3. Нажмите **"Remove App"** (Удалить программу)
4. Подтвердите **"Delete App"** (Удалить)

#### Шаг 2: Установите через Xcode
1. Подключите iPhone к Mac через кабель
2. Откройте терминал:
```bash
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat/mobile"
open ios/Runner.xcworkspace
```

3. В Xcode:
   - Убедитесь что в верхней панели выбран ваш iPhone (не Simulator)
   - Нажмите **▶ Run** (или Cmd+R)
   - Подождите пока приложение соберётся и установится (~30 сек)

4. Приложение автоматически запустится на iPhone

#### Шаг 3: Проверьте версию
1. Войдите под **admin/admin**
2. Откройте **Profile → View as User**
3. Нажмите на любого пользователя
4. **Внимательно посмотрите** что происходит

### Вариант 2: Через командную строку

```bash
# 1. Очистка кэша Flutter
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat/mobile"
flutter clean

# 2. Получение зависимостей заново
flutter pub get

# 3. Сборка для iOS
flutter build ios --release

# 4. Запуск на устройстве
flutter run --release
```

Когда приложение запустится на iPhone:
- Удалите старую версию вручную
- Закройте приложение (свайп вверх из нижней части экрана)
- Запустите заново с главного экрана

### Вариант 3: Debug режим (для отладки)

Если хотите видеть логи в реальном времени:

```bash
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat/mobile"
flutter run --verbose
```

Это запустит приложение в debug режиме с полными логами в терминале.
Вы увидите ВСЕ print() сообщения прямо в терминале!

## После переустановки

### Что должно произойти:

1. **Войдите:** admin/admin
2. **Profile → View as User**
3. **Нажмите на Simon Clarke**
4. **Диалог подтверждения появится**
5. **Нажмите "View as User"**

### Что вы ДОЛЖНЫ увидеть:

- ✅ Приложение переходит на **HomeScreen**
- ✅ **Красный баннер** появляется сверху
- ✅ В баннере: "Viewing as: Simon Clarke"
- ✅ Avatar меняется на Simon Clarke
- ✅ Имя меняется на Simon Clarke
- ✅ Кнопка **"Exit View"** в баннере

### Если НЕ работает:

Запустите в debug режиме и скопируйте логи:

```bash
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat/mobile"
flutter run --verbose > /tmp/flutter-logs.txt 2>&1 &

# Воспроизведите проблему в приложении

# Посмотрите логи:
cat /tmp/flutter-logs.txt | grep -E "👆|🎭|📦|📞|📊|✅|❌|🎨"
```

## Checklist перед тестированием

- [ ] iPhone подключен к Mac
- [ ] Старое приложение удалено с iPhone
- [ ] Flutter clean выполнен
- [ ] Приложение установлено через Xcode или flutter run
- [ ] Вошли под admin/admin
- [ ] Интернет работает на iPhone

## Если всё ещё не работает

Попробуйте **Debug режим с логами**:

### Шаг 1: Запустите с логированием
```bash
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat/mobile"
flutter run 2>&1 | tee /tmp/flutter-debug.log
```

### Шаг 2: Воспроизведите проблему
1. В приложении: Profile → View as User
2. Нажмите на пользователя
3. Нажмите "View as User" в диалоге

### Шаг 3: Проверьте логи
```bash
# В другом терминале:
tail -100 /tmp/flutter-debug.log | grep -E "👆|🎭|📦|📞|📊|✅|❌|🎨|Error|Exception"
```

Отправьте мне результат!

## Важные моменты

### 1. Release vs Debug режим
- **Release** (`flutter build ios --release`) - финальная версия, логи минимальны
- **Debug** (`flutter run`) - версия для разработки, ВСЕ логи видны

### 2. Hot Reload не работает для Provider'ов
- После изменения Provider'ов НУЖНА полная переустановка
- Hot Reload (R в консоли) работает только для UI

### 3. Xcode Console vs Flutter Console
- **Xcode Console** - логи от iOS системы
- **Flutter Console** (`flutter run`) - логи от Flutter приложения (лучше для отладки)

## Рекомендуемый способ СЕЙЧАС

**Используйте Debug режим для полной видимости:**

```bash
# 1. Удалите приложение с iPhone вручную
# 2. Запустите в debug:
cd "/Users/svetanaborvinskaia/Desktop/Lone Star Chat/mobile"
flutter run

# 3. Когда появится "Flutter run key commands"
#    приложение запущено
# 4. Воспроизведите проблему
# 5. Все логи будут прямо в терминале!
```

Вы увидите ВСЁ что происходит в реальном времени! 🎯

---

**НАЧНИТЕ С ВАРИАНТА 3 (Debug режим)** - это самый быстрый способ понять что не так!
