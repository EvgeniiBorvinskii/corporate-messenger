# 🔐 ДОСТУП К СЕРВЕРУ 5.249.160.54 - ИНСТРУКЦИИ

## Проблема доступа

**SSH порт 22**: ЗАКРЫТ ❌  
**Ping**: НЕ ОТВЕЧАЕТ ❌  
**HTTP API порт 80**: ДОСТУПЕН ✅

---

## ✅ Что работает:

API сервер **доступен** через HTTP на порту 80:
```bash
curl http://5.249.160.54:80/api/version/check
# Ответ: 200 OK
```

---

## ⚠️ Что НЕ работает с моего компьютера:

1. **SSH доступ закрыт**:
```bash
ssh root@5.249.160.54
# Connection refused (порт 22 закрыт)
```

2. **Ping не проходит**:
```bash
ping 5.249.160.54
# 100% packet loss
```

Это означает что:
- Либо файрвол блокирует SSH и ICMP
- Либо SSH работает на другом порту
- Либо доступ разрешен только с определенных IP адресов

---

## 🔧 ВАРИАНТЫ РЕШЕНИЯ:

### Вариант 1: Вы проверите API сами

Подключитесь к серверу через ваш обычный терминал (PuTTY, Terminal и т.д.) и выполните эти команды:

```bash
# 1. Подключиться к серверу
ssh root@5.249.160.54
# Пароль: 54654250pS3123

# 2. Найти запущенный процесс Node.js
ps aux | grep node

# 3. Найти файл сервера
find /opt /srv /root -name "*server*.js" -type f 2>/dev/null

# 4. Проверить эндпоинт назначения ролей (изнутри сервера)
curl -s http://localhost:80/api/admin/employees/2/roles \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -X PUT \
  -H "Content-Type: application/json" \
  -d '{"roles": ["sales"]}'

# 5. Проверить эндпоинт удаления работника
curl -s http://localhost:80/api/admin/employees/999 \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -X DELETE

# 6. Проверить эндпоинт создания работника
curl -s http://localhost:80/api/admin/employees \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","full_name":"Test User","password":"123456"}'

# 7. Проверить DM чаты
curl -s http://localhost:80/api/messages/dm/2/messages \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Если эндпоинты возвращают 404** - значит их нужно добавить!

---

### Вариант 2: Откройте SSH порт для моего IP

Если вы хотите чтобы я сам добавил эндпоинты, нужно:

1. **Узнать мой IP адрес** (я работаю с вашего компьютера, так что это ваш текущий IP)
2. **Открыть SSH порт 22** на файрволе сервера для этого IP
3. **ИЛИ** сказать мне на каком порту работает SSH (если не 22)

Тогда я смогу подключиться и добавить недостающие эндпоинты.

---

### Вариант 3: Я создам файлы для загрузки на сервер

Я могу создать готовые файлы с кодом эндпоинтов, которые вы:
1. Загрузите на сервер через SFTP/SCP
2. Выполните на сервере
3. Перезапустите backend

---

## 📋 ЧТО НУЖНО ПРОВЕРИТЬ:

Выполните на сервере (через SSH) и пришлите мне результаты:

```bash
# 1. Проверить существование эндпоинтов в коде
grep -n "PUT.*employees.*roles" /opt/lone-star-chat/backend/*.js
grep -n "DELETE.*employees" /opt/lone-star-chat/backend/*.js
grep -n "POST.*employees" /opt/lone-star-chat/backend/*.js
grep -n "messages/dm" /opt/lone-star-chat/backend/*.js

# 2. Посмотреть структуру файлов
ls -lah /opt/lone-star-chat/backend/

# 3. Проверить запущенный процесс
ps aux | grep node | grep -v grep

# 4. Посмотреть последние логи
journalctl -u lone-star-chat -n 50 --no-pager
```

---

## 🎯 СЛЕДУЮЩИЙ ШАГ:

**Выберите один из вариантов и скажите мне:**
1. Результаты проверки API (вариант 1)
2. На каком порту SSH или откройте порт 22 (вариант 2)
3. Или я создам файлы для загрузки (вариант 3)

После этого я смогу добавить недостающие 4 API эндпоинта и все заработает! 🚀
